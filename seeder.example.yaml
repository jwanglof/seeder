# seeder.yaml — example configuration.
#
# Auto-detected when present in the working directory, or pass
# --config <file> to point at one explicitly.
#
# Precedence: CLI flag > seeder.yaml > built-in default.

version: 1

# Default rows per table (--rows on the command line wins and overrides
# per-table values below).
rows: 1000

# Default RNG seed (--seed on the command line wins). Leave unset for a
# time-based seed.
seed: 42

# TRUNCATE before insert (--truncate on the command line wins).
truncate: false

# Locale for inferred text values (en, ja). --locale on the command line wins.
locale: en

# Per-table overrides.
tables:
  users:
    rows: 10000
    # Per-column overrides. Use `generator` to force a built-in (see
    # `seeder --help` for the list) or `value` to pin a literal. Set
    # exactly one of the two per column.
    columns:
      email:
        generator: Email
      country:
        value: JP
  audit_log:
    exclude: true
